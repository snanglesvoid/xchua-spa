import { CaptionDirective } from './caption.directive';

describe('CaptionDirective', () => {
  it('should create an instance', () => {
    const directive = new CaptionDirective();
    expect(directive).toBeTruthy();
  });
});
