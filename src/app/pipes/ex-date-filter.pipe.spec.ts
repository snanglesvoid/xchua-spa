import { ExDateFilterPipe } from './ex-date-filter.pipe';

describe('ExDateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExDateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
