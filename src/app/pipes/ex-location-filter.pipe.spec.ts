import { ExLocationFilterPipe } from './ex-location-filter.pipe';

describe('ExLocationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExLocationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
