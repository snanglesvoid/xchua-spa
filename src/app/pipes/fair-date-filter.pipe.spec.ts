import { FairDateFilterPipe } from './fair-date-filter.pipe';

describe('FairDateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FairDateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
